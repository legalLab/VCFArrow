CXX_STD = CXX20

# Use Arrow's own build system flags (critical), not R (# out)
#PKG_CPPFLAGS += $(shell ${R_HOME}/bin/Rscript -e "arrow::cppflags()")
#PKG_LIBS += $(shell ${R_HOME}/bin/Rscript -e "arrow::ldflags()")

PKG_CPPFLAGS += $(shell pkg-config --cflags arrow-dataset)
PKG_LIBS += $(shell pkg-config --libs arrow-dataset)
PKG_LIBS += -lz
